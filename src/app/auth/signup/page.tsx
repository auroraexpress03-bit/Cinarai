import { SignUpForm } from '@/components/auth/SignUpForm';

export const metadata = {
  title: 'Daftar - CINARAI',
  description: 'Daftar akun CINARAI baru',
};

export default function SignUpPage() {
  return <SignUpForm />;
}
