import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Masuk - CINARAI',
  description: 'Masuk ke akun CINARAI Anda',
};

export default function LoginPage() {
  return <LoginForm />;
}
